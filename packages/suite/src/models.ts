// Model-derivation semantics now live in @everdict/domain — re-architecture P1a compat re-export
// (removed in the P4 sweep). New code should import @everdict/domain directly.
export { type ScorecardModels, scorecardModels } from "@everdict/domain";
