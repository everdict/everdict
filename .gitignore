node_modules/
dist/
.turbo/
coverage/

# env / secrets — never commit (no secret in git)
.env
.env.*
!.env.example
*.local
*.pem
*_credentials

# editor / os
.DS_Store
*.log

# live e2e artifacts (scripts/live/*.mjs 가 리포 루트에 쓰는 스크린샷 등 — 커밋 대상 아님)
/e2e-*.png

# electron packaging artifacts (apps/desktop)
apps/desktop/bundle/
apps/desktop/release/
apps/web/.next-dev/
