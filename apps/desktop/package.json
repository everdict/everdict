{
  "name": "@assay/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Assay desktop — web parity shell + resident self-hosted runner",
  "author": "Assay",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "pnpm build && electron .",
    "package": "node esbuild.mjs && electron-builder --publish never"
  },
  "dependencies": {
    "@assay/runner-core": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "electron": "^43.0.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.1"
  }
}
