// The display-image validation rule now lives in @everdict/domain — re-architecture P2d compat
// re-export (removed in the P4 sweep). New code should import @everdict/domain directly.
export { validateImageRef } from "@everdict/domain";
