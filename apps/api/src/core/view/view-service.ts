// ViewService now lives in @everdict/application-control — re-architecture P2d compat re-export (removed in the P4 sweep).
export {
  type CreateViewInput,
  type UpdateViewInput,
  ViewService,
  type ViewServiceDeps,
} from "@everdict/application-control";
