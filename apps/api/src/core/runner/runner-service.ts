// RunnerService now lives in @everdict/application-control — re-architecture P2d compat re-export (removed in the P4 sweep).
export {
  type PairRunnerBody,
  PairRunnerBodySchema,
  RUNNER_CAPABILITIES,
  RunnerService,
} from "@everdict/application-control";
