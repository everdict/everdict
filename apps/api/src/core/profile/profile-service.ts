// ProfileService now lives in @everdict/application-control — re-architecture P2d compat
// re-export (removed in the P4 sweep). New code should import @everdict/application-control directly.
export { ProfileService } from "@everdict/application-control";
