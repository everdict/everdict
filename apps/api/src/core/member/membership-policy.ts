// MembershipPolicy (the last-admin invariant) now lives in @everdict/domain — re-architecture P1c
// compat re-export (removed in the P4 sweep). New code should import @everdict/domain directly.
export { MembershipPolicy } from "@everdict/domain";
