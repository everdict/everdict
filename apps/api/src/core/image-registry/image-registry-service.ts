// image-registry-service now lives in @everdict/application-control — re-architecture P2d compat re-export (removed in the P4 sweep).
export {
  type ImagePushCredentials,
  ImageRegistryService,
  type ImageRegistryServiceDeps,
  type ImageRegistryView,
} from "@everdict/application-control";
