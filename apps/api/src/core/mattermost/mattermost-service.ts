// mattermost-service now lives in @everdict/application-control — re-architecture P2d compat re-export (removed in the P4 sweep).
export {
  type MattermostConfigView,
  MattermostService,
  type MattermostServiceConfig,
} from "@everdict/application-control";
