// CommentService now lives in @everdict/application-control — re-architecture P2d compat re-export (removed in the P4 sweep).
export {
  COMMENT_RESOURCE_TYPES,
  type CommentResourceType,
  CommentService,
  type CommentServiceDeps,
} from "@everdict/application-control";
