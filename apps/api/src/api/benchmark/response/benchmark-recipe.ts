import { BenchmarkAdapterSpecSchema } from "@everdict/datasets";

// GET /benchmark-recipes/:id/versions/:version 200 — the full recipe spec. SSOT: @everdict/datasets.
export const BenchmarkRecipeResponseSchema = BenchmarkAdapterSpecSchema;
