// The wire DTO now lives in contracts/wire — re-architecture P0c compat re-export (removed in the P4 sweep).
export { InstallationWithReposSchema, GithubAppDetailViewSchema } from "@everdict/contracts/wire";
