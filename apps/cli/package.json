{
  "name": "@everdict/cli",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "bin": { "everdict": "./dist/main.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@everdict/agent": "workspace:*",
    "@everdict/backends": "workspace:*",
    "@everdict/core": "workspace:*",
    "@everdict/orchestrator": "workspace:*",
    "@everdict/runner-core": "workspace:*",
    "@everdict/suite": "workspace:*",
    "@everdict/topology": "workspace:*",
    "zod": "^3.24.1"
  }
}
