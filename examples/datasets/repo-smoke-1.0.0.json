{
  "id": "repo-smoke",
  "version": "1.0.0",
  "description": "first-party benchmark — basic repo-environment smoke (file create/edit). Harness-agnostic: any harness is evaluated on the same cases.",
  "tags": ["benchmark", "repo"],
  "cases": [
    {
      "id": "create-file",
      "env": { "kind": "repo", "source": { "files": {} } },
      "task": "Create a file named ok.txt whose contents are exactly: done",
      "graders": [{ "id": "steps" }, { "id": "cost" }, { "id": "latency" }],
      "timeoutSec": 300,
      "tags": ["smoke"]
    },
    {
      "id": "edit-readme",
      "env": { "kind": "repo", "source": { "files": { "README.md": "# demo\n" } } },
      "task": "Append a line that reads 'updated by agent' to the end of README.md",
      "graders": [{ "id": "steps" }, { "id": "cost" }],
      "timeoutSec": 300,
      "tags": ["smoke"]
    }
  ]
}
