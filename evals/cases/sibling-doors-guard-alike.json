{
  "id": "sibling-doors-guard-alike",
  "why": "rule `ci` — within one resource, an entity-naming door carries the guards its siblings carry; reading for it works exactly once, because the door written after the lesson is the one that never learned it. ⚠️ 2026-09-07: went GREEN under its drill as an open-ended question a correct generic answer satisfied. Rewritten as a counter-intuitive yes/no where the intuitive answer is WRONG and only the lesson supplies the right one: matching the siblings reads as complete; the door written AFTER the lesson is the one that never learned it.",
  "subject": [".claude/rules/ci.md"],
  "neutralize": ["guard-siblings", "the door written AFTER the"],
  "allowedTools": "Read,Grep,Glob",
  "timeout": 180,
  "prompt": "I added one route to an existing resource and gave it the same auth guard the others already have. Anything else owed before I push?",
  "expect": {
    "mustMatch": ["guard-siblings|baseline"]
  }
}
