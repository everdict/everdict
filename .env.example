# 복사해서 .env 로 (.env 는 gitignore). 대부분 비워둬도 됩니다 —
# LocalDriver 는 이 머신의 claude 구독 로그인을 그대로 사용합니다.

# 로그인이 없는 샌드박스 잡(Nomad/K8s 등)·CI 에서 claude-code 를 돌릴 때만 필요:
# ANTHROPIC_API_KEY=

EVERDICT_TIMEOUT_SEC=300
