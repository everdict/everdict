# Finding dispositions

What happened to what the reviewer and the scanner reported. Written by
`pnpm findings --record`, and COMMITTED: a judgement nobody else can read is one the next person makes again.

A finding is `real` (it named a defect), `false-positive` (it did not), or `carried` (real, and deliberately
not fixed here — an intent holds it). Precision counts real and carried against the total; carrying a finding
is not the same as disagreeing with it.

## ⚠️ Read the 97% with what produced it

A precision this high is not a claim that the reviewer is nearly always right. It is a fact about WHICH
findings are in the corpus and WHEN they were graded, and both bias it upward:

- **The corpus is findings somebody already acted on.** Almost every `real` below was verified by opening the
  file and finding the repair already there, often with the incident recorded in the code's own comment. A
  reviewer's findings that nobody agreed with tend not to survive as a report at all.
- **They were graded in one pass, months after the fact.** "Was this defect repaired?" is a much easier
  question than "is this a defect?", and it is the question a late grading actually answers. Grading at the
  moment of the review — while the disagreement is live — is the harder and more informative version.

What the number IS good for is its direction over time and its counter-metric: `false-positive` counts the
reviewer's cost, and two of sixty-three is the measurement that was missing entirely before this ledger. Read
it beside how many findings each run raises, never alone.

<!-- entries below, newest last -->
- 2026-09-07 · review@2ebc79ca11ef · `evals/run.mjs` · **real** — the stamp condition read failed===0 and ignored inconclusive, so an unanswered run could certify; fixed in d4aa2e3c
- 2026-09-07 · review@2ebc79ca11ef · `scripts/bands/watch.mjs` · **carried** — the YAML reader mis-types a threshold into a string instead of refusing it; the honest repair is a parse that refuses, filed as intent 2026-09-06-three-findings-the-review-left
- 2026-09-07 · review@2ebc79ca11ef · `scripts/check-swallowed-reads.mjs` · **real** — the header claimed a baseline of 83 against a file summing to 105 across 61 files; corrected in d4aa2e3c
- 2026-09-07 · review@2ebc79ca11ef · `scripts/ci-commits.mjs` · **carried** — the history exclusion pathspec is duplicated instead of importing CONFIG_PATHSPEC; filed as intent 2026-09-06-three-findings-the-review-left
- 2026-09-07 · review@d4aa2e3c46df · `packages/application-control/src/evolution/campaign-service.ts` · **carried** — verifyExamControl reads card.manifest.verdictPolicy directly instead of through ExecutionPlan; pre-existing, filed for the same intent
- 2026-09-07 · review@d4aa2e3c46df · `scripts/check-swallowed-reads.mjs` · **carried** — the ratchet globs .ts and never .tsx, so 50 occurrences across 29 tsx files are invisible; widening moves the baseline so it needs its own change
- 2026-09-07 · scan@contracts · `packages/contracts/src/execution/git-auth.ts` · **carried** — unscoped http.extraheader leaks the installation token to any host a submodule or redirect names; verified by reading and filed as intent 2026-09-05-scan-contracts-outbound-credentials
- 2026-09-07 · scan@contracts · `packages/contracts/src/infra/outbound-target.ts` · **carried** — isPrivateAddress is blind to IPv4-mapped IPv6, so an SSRF guard admits ::ffff:169.254.169.254; verified and filed in the same intent
- 2026-09-07 · scan@api · `apps/api/src/api/harness/harness.routes.ts` · **false-positive** — the scanner claimed this door parses before authorizing unlike its siblings; the siblings at lines 39 and 104 do the same, so the claim was contradicted by the file it cited
- 2026-09-07 · review@033e965b8cbf · `evals/run.mjs` · **real** — the pass cache keyed on HEAD alone, so an uncommitted edit to the very file a case tests was answered from a stale pass; it keys on a configDigest over the overlaid bytes now
- 2026-09-07 · review@062097150d5c · `.claude/settings.json` · **real** — the deny entry carried a './' prefix no other pattern in the file uses; corrected in fbec5a0b, and the current line reads Read(secrets/**)
- 2026-09-07 · review@062097150d5c · `docs/README.md` · **real** — the four new index links were the one citation form docs-check demanded and never verified; the check reads absolute github URLs into this repository as path assertions now (87d42eae)
- 2026-09-07 · review@062097150d5c · `docs/architecture/harness-observability.md` · **real** — the page said telemetry was opt-in while the same change turned it on by default; the page describes the SessionStart sink instead now
- 2026-09-07 · review@062097150d5c · `scripts/check-lesson-evals.mjs` · **real** — the ghost condition only fired when EVERY named id was missing, so one real case masked a renamed-away one; the whole id-guessing mechanism was replaced in bdfff603
- 2026-09-07 · review@300fe3865752 · `.claude/settings.json` · **false-positive** — driven, not read: `curl --version` through the Bash tool is DENIED, so `Bash(curl *)` does match a real invocation and the colon-wildcard form is not required
- 2026-09-07 · review@300fe3865752 · `scripts/check-lesson-evals.mjs` · **real** — the denial window swallowed 'there was none before; now there is X', so a real claim skipped its existence check; the mechanism was replaced in bdfff603
- 2026-09-07 · review@300fe3865752 · `scripts/scan/run.mjs` · **real** — the dismissed set was parsed and never consulted, so a dismissed finding came back as new; the printer marks it now and the code says so at the line
- 2026-09-07 · review@300fe3865752 · `scripts/review/run.mjs` · **real** — a single over-cap file diff was truncated silently under a comment claiming truncation is reported; truncatedFiles collects and reports it now
- 2026-09-07 · review@300fe3865752 · `scripts/hooks/gate-decision.mjs` · **real** — an unreadable CI ledger answered before the release arm, so a refused release reported the cheaper gate; the release check runs first now and the comment says why
- 2026-09-07 · review@39597b37cb52 · `scripts/check-lesson-evals.mjs` · **real** — same denial-window defect from a third angle; closed by replacing the id-guessing mechanism in bdfff603
- 2026-09-07 · review@4ff20f064d7d · `scripts/bands/bands.yaml` · **real** — the tiers block sat above the line the parser started at, so an operator editing tier actions or the 2-sigma tool list changed nothing; watch.mjs parses it now and records the incident
- 2026-09-07 · review@4ff20f064d7d · `evals/cases/ci-local-before-push.json` · **real** — the case asserted on phrasing a correct answer could legitimately use, the false-failure trap a sibling case had already documented; the case was retired when the suite went to four
- 2026-09-07 · review@4ff20f064d7d · `scripts/hooks/gate-decision.mjs` · **real** — PRODUCT_PATHS named only packages and apps, so a push that changed only scripts/ - the gate, the reviewer and ci-local themselves - required no review; scripts is in the list now
- 2026-09-07 · review@4ff20f064d7d · `scripts/check-lesson-evals.mjs` · **real** — at-least-one-exists certified a lesson naming one live case and one deleted one; the mechanism was replaced in bdfff603
- 2026-09-07 · review@669b1bc2f335 · `evals/run.mjs` · **real** — a cached pass survived into --drill, so the removal drill could certify a neutralized tree it never ran; a drill neither reads nor writes the cache now
- 2026-09-07 · review@669b1bc2f335 · `scripts/review/run.mjs` · **real** — an unparseable reply was cached and folded in like a result while the report dropped the flag; unstructuredParts is counted and reported now, as the scanner already did
- 2026-09-07 · review@7d71a43fd56f · `packages/application-control/src/evolution/exam-proof.ts` · **real** — a positive control read any measured value>0 as a pass, so a cost or latency score proved the exam; the file now records that release by name and asks the platform's pass
- 2026-09-07 · review@7d71a43fd56f · `packages/domain/src/evolution/round-brief.ts` · **real** — inherited findings were redacted against the current campaign's held-out ids while informedBy had no cross-frame validation; the hole is named and closed in the file
- 2026-09-07 · review@804900077353 · `evals/run.mjs` · **real** — a red run exited before appending to history.jsonl, so the eval-pass-rate band watched a series that could not contain a regression; the append precedes every exit now
- 2026-09-07 · review@804900077353 · `evals/README.md` · **real** — the README promised a drill-stale marker and a startup refusal that no code implemented; the page explains why there is none instead
- 2026-09-07 · review@804900077353 · `scripts/design/run.mjs` · **real** — the spec template emitted neither Policies nor Concerns while intent-chain hard-failed a spec missing either, so the design pass could not produce a valid spec; both lines are emitted now
- 2026-09-07 · review@87973c8ed462 · `apps/web/src/app/[workspace]/views/[id]/page.tsx` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `apps/web/src/app/[workspace]/harnesses/page.tsx` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `.claude/rules/protocol.md` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `apps/web/src/entities/capability/model/schema.ts` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `apps/web/src/entities/harness/model/schema.ts` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `apps/web/src/features/analyze-scorecards/model/analysis.ts` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `apps/web/src/features/manage-schedules/ui/schedule-card.tsx` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `apps/web/src/features/manage-issue/ui/issue-status-control.tsx` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `apps/web/src/features/set-agent-model/ui/agent-model-picker.tsx` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `examples/bundles/spreadsheetbench/scripts/sbench_digest.py` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `examples/bundles/spreadsheetbench/scripts/sbench_grade.py` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `examples/bundles/spreadsheetbench/Dockerfile` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `packages/application-control/src/evolution/campaign-service.ts` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `packages/domain/src/evolution/campaign-gate.ts` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `packages/application-control/src/comment/comment-service.ts` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `packages/contracts/src/records/ownership.ts` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `packages/application-control/src/scorecard/scorecard-service.ts` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `packages/application-control/src/run-suite.test.ts` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `packages/backends/src/orchestrators/k8s.test.ts` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `packages/db/src/tracker/issue-store.test.ts` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@87973c8ed462 · `docs/architecture/rearchitecture/domains/failure.md` · **real** — from the review of a batch that was not merged: the current tree keeps the English source comments, the sbench_position parser, the exam positive control and the protocol section this finding said were being removed. The review is why it did not ship
- 2026-09-07 · review@bca683b928e7 · `scripts/check-language-policy.mjs` · **real** — the Korean exemption covered the whole evals/cases directory rather than the one named stimulus; the entry names the file and the header records the widening
- 2026-09-07 · review@d2ed5a9e54b2 · `evals/run.mjs` · **real** — the config digest hashed evals/.results and history.jsonl, which every run rewrites, so the cache could never hit; WRITTEN_BY_A_RUN excludes them and the file says so
- 2026-09-07 · review@d2ed5a9e54b2 · `scripts/hooks/pre-push-gate.mjs` · **real** — a failed git read made configChanged false, which sat the eval and review arms out rather than denying; a failed read means CHANGED now, and the line records it
- 2026-09-07 · review@e81a885eb159 · `.claude/settings.json` · **real** — a bound composed with an unbounded neighbour: the deny list stops Read and two Bash prefixes while the general shell reaches the same bytes. Answered by declaring it - rule ci and the declared-limits page now say plainly that this is a speed bump and not containment
- 2026-09-07 · scan@adapters · `packages/db/src/workspace/workspace-store.ts` · **real** — and worse than reported: one entry named a table dropped in migration 0046, so the delete rejected outright. The sweep is derived from the schema now, certified as TRUST-194
- 2026-09-07 · scan@agent-runtime · `packages/agent-runtime/src/kernel/loop.ts` · **real** — intrinsic was applied to the whole spawn array, exempting spawn_teammate and list_teammates from both envelope guards; it is decided per tool now, with the set pinned by a counterexample
- 2026-09-07 · scan@agent-runtime · `packages/agent-runtime/src/context/compaction.ts` · **real** — discovery was re-derived from a transcript compaction is licensed to edit, so a search's tools left the outbound list mid-procedure; the kernel holds the set now
- 2026-09-07 · scan@agent · `apps/agent/src/agent-activation.ts` · **real** — a bare catch turned a transient store failure into a permanent skip, against the function's own doc comment; only NotFoundError is a permanent answer now
- 2026-09-07 · scan@agent · `apps/agent/src/server.ts` · **real** — the member branch parsed a schema carrying causedBy, so any member could silence one named agent; the member surface declares no platform-authored field now
- 2026-09-07 · scan@domain · `packages/domain/src/trace/spans-to-events.ts` · **real** — a producer's forged everdict.plane minted the platform's own infra record; the attribute is stripped at the pull boundary now
- 2026-09-07 · scan@execution · `packages/backends/src/orchestrators/k8s.ts` · **real** — the verifier lane reserved and created a namespace before the pure refusal that would turn the dispatch away; the refusal comes first now, as it does on the agent lane and on Nomad
- 2026-09-07 · review@96fb4521c952 · `evals/README.md` · **real** — the same push retired the fourth case and left the README saying four, in the sentence that calls the count load-bearing; corrected to three with what the twelfth retirement cost, and the drill certificate cited beside it
- 2026-09-07 · review@1c44ec88ea9f · `packages/db/src/workspace/workspace-store.ts` · **real** — the derived sweep keyed its map by table name on the strength of no table carrying both scope columns today, so a future one would have half its rows survive a delete that reports success — one entry per (table, column) now, with a counterexample
- 2026-09-07 · review@1c44ec88ea9f · `scripts/fix-proof.mjs` · **real** — the ran-nothing safeguard was defeated by its own grouping: files ran per package in one call, so a red sibling certified a skipped trust test as proof — one file per invocation now
- 2026-09-07 · review@1dfdb066a933 · `scripts/check-gate-order.mjs` · **real** — the census matched the bare substring gate(, which resolveHarnessDelegate( ends in — masking an ungated door in one direction and inflating the baseline in the other, in a check already wired as CI-required; a word boundary now, and the census is identical either way, so the repair changed no verdict
- 2026-09-09 · review@ad529f43eece · `scripts/fix-proof.mjs` · **real** — the `declined` arm was gated on `touched.length === 0`, so a commit whose only test file is a declared `Fixture-only:` AND which carries `Regression-test: none — <why>` was answered `violation` under a message telling it to add the line it already had; the gate was dropped and the four arms driven again
- 2026-09-09 · review@ad529f43eece · `apps/api/src/main.ts` · **carried** — the oracle attestation compares the listing's `baselineSha`/`candidateSha` against the ones it asked for, and this composition builds them by spreading `...commits`, its own input, so the check cannot fail. It is not false today (the commits come from the build ledger, which is what R1 changed) but it is a guard that would not notice its own premise being removed. Honoring it needs `listPullRequestChanges` to return the commits GitHub actually compared, which its adapter does not read; carried into the review document's limits rather than half-fixed
- 2026-09-09 · review@ad529f43eece · `packages/db/src/evolution/campaign-store.ts` · **false-positive** — `assertFamilyCapacity` charges an OPEN sibling its whole `budget.maxRounds` while `ExperimentFamily.consumed` counts only reservations that happened, and the reviewer read that as a refusal over budget nobody spent. It is the declared policy, written in `docs/architecture/evolution-review-follow-up.md` by the commit that built the door: opening a continuation RESERVES its maximum allocation alongside open siblings. An allocation is not a spend, and it may not be — the pre-registered family size bounds how many tests the chain corrects for, so admitting N open siblings against unspent budget would let the family exceed the size it registered, which is the whole thing `heldOutFamilySize` freezes. The genuine duplication under this finding is the UNDECLARED-root divergence, reported separately at `review@c22ee9e93cab` and repaired there
- 2026-09-10 · review@c22ee9e93cab · `packages/db/src/evolution/experiment-family.ts` · **real** — the family's held-out size was read at two doors with opposite answers to the one value the schema still permits, an UNDECLARED root: the chain door refused while blaming a budget shortfall it had not measured (`{reserved, limit: undefined}`), and the reservation door defaulted to `budget.maxRounds` and admitted. `experimentFamilyLimit` owns the fact now, three-valued rather than defaulted, and each door STATES what it does with the absence — the chain door refuses a family it cannot account for, the reservation door lets a legacy root spend the budget it froze (rule `protocol` L3)
- 2026-09-10 · review@ad7c0da7db4e · `scripts/hooks/pre-push-gate.mjs` · **real** — the stamp-inheritance key was `(tree, parent tree)` and a `--amend -m` reword keeps both, so `chore(x): tidy imports` could become `fix(x): imports were broken` and inherit a certification that never ran the fix-proof rule its new message demands. The key hashes the commit MESSAGE now, and rule `ci` records why
- 2026-09-10 · review@ad7c0da7db4e · `scripts/ci-commits.mjs` · **real** — the same defect in the half that RUNS the proof: a commit matched by tree was `continue`d past, so `verdictFor`/`proveInWorktree` never executed for the reworded commit. One key, computed once, in both readers
- 2026-09-10 · review@ad7c0da7db4e · `scripts/review/run.mjs` · **real** — resuming from the newest stamped ancestor would have dropped a file whose chunk came back as PROSE from every later range forever: the part was recorded `unstructured` and still stamped (deliberately — a formatting failure is not a finding), and the resume then walked past it. A stamp carrying `unstructured=<n>` is skipped as a resume point, so that region is read again
- 2026-09-10 · review@579879d79c05 · `.claude/rules/ci.md` · **real** — both gate invariants above changed the push gate's behaviour and neither reached the rule that is injected while editing those scripts, which is the drift class this repo has already paid for twice. The unstructured-resume half was written up with the change; the tree-equivalence key was not, and is now
- 2026-09-10 · review@50d062e350e0 · `packages/application-control/src/evolution/campaign-service.ts` · **real** — the review of the batch that unified two readers of `significance.heldOutFamilySize` found a THIRD, with its own inline `?? budget.maxRounds`, in the non-inferiority computation. Unreachable — `verdictOf`'s only caller refuses an undeclared family a hundred lines above — but an unreachable fallback is a premise nobody re-checks. Deleted rather than taught the owner: the caller has already proved the value and now passes it as a required parameter
- 2026-09-11 · review@a8240d3d9973 · `docs/architecture/evolution-review-2026-09-10.md` · **real** — the batch repaired a live-reproduced P0 with production code, new counterexamples, an architecture document and a lesson, and filed no `intent/`. `pnpm intent-chain` cannot see the omission (it validates ordering INSIDE directories that already exist), and the reviewer's sharpest point is the precedent: the intent one day earlier was filed for the same reason, on the same kind of change. Filed as `intent/2026-09-10-diverged-comparisons-and-uncertified-scenarios/`
- 2026-09-11 · review@e3111406cee4 · `intent/2026-09-10-diverged-comparisons-and-uncertified-scenarios/intent.md` · **real** — that intent holds two defects and `Shipped:` names one commit, so a reader auditing the trust-scenario half opens 4573eba0 and finds only the oracle repair. `Shipped:` takes exactly one sha by the check's own grammar, so the second (`dd5c154b`) is named on its own line beside it, with why the two were not split
- 2026-09-11 · review@1490d019f6ed · `scripts/check-trust-certified.mjs` · **real** — the check written to stop a partial certification reading as a full one did exactly that: it compared the marker's SHA and never its recorded scope, so `trust-suite.mjs`'s own documented named-subset run (`apps/api/src/trust` alone) wrote a marker that printed "nothing in the certified scope has changed since" while every `packages/**` and `apps/agent/**` scenario had never run at that commit. Coverage is checked now — broader still covers, a missing include or an extra exclude is named, an empty scope is treated as none — and the certified sha must be an ANCESTOR rather than merely an object the repository still holds, which a rewrite orphans
- 2026-09-11 · review@985613611bc8 · `findings/DISPOSITIONS.md` · **false-positive** — it read the entry above it as `review@1490d019cee4` and reported the key as fabricated. The line says `review@1490d019f6ed`, which is `.git/everdict-review-1490d019f6ed.json`: the review run at HEAD `1490d019f6ed700aaaef41926838916cfa0d5076`, whose single finding is the `check-trust-certified.mjs:109` one that entry grades. `pnpm findings` matched it, which is the mechanical answer — a key naming no report shows up as an extra graded line over an unchanged reported count, and it did not. The reviewer's rule is right and worth keeping; it applied it to characters that are not there
- 2026-09-11 · scan@contracts · `packages/contracts/src/execution/eval-case.ts` · **real** — `sourceTraceId` is "the platform trace this result was scored FROM", written only by the control plane's own pulls, and it was absent from `PLATFORM_STAMPED_RESULT_FIELDS`, so a self-hosted runner could name any trace id through `submit_job_result` and have it stored as canonical AND exported verbatim to four external sinks as the route back from a verdict to its evidence. Verified by reading every link the scan named: the declaration, both writers, both untrusted doors, and the consumers. It sat TWO FIELDS below the arch-review 122 comment stating this law. Stripped now, with a counterexample seen red; the structural half — that `untrusted-ingress` checks the DOOR and nothing re-asks the LIST — is filed as `intent/2026-09-11-every-case-result-field-declares-its-author/`
- 2026-09-11 · review@a041f8ef59c4 · `deploy/compose/README.md` · **real** — the workflow-deletion batch left this file describing a tag-triggered image publish, so an operator would push `api-v1.2.0` and get no image and no error. ⚠️ `pnpm docs-check` did not and could not catch it: its corpus is `docs/**` and `.claude/**`, and `deploy/**` is outside it — the citation was even in backticks. The table is now the naming contract a manual `docker buildx --push` must honour, said in those words
- 2026-09-11 · review@a041f8ef59c4 · `docs/architecture/browser-profiles.md` · **real** — one paragraph said the mirror workflow was deleted and then told the reader to re-run it. A deletion note that leaves the instruction standing beside it is worse than not noting it
- 2026-09-11 · review@a041f8ef59c4 · `scripts/check-trust-certified.mjs` · **real** — two error messages still named `trust-fast.yml` after the scope moved to `package.json`, so the one person who ever reads them — somebody debugging scope drift — would grep for a file this same batch deleted. The comment block above them documented the move; the strings did not
- 2026-09-11 · review@a4de0c0bc949 · `CLAUDE.md` · **real** — and the sharpest finding of the session. The rewritten sentence named `EVERDICT_TRUST_DATABASE_URL` as the trust-suite skip gate; `trust-context.ts` says in its own comment that `EVERDICT_TRUST_SUITE=1` runs the suite AT ALL and the URLs only choose what a scenario drives once inside it, so a developer setting the URLs alone still sees every file SKIPPED. ⚠️ The agent-eval case's `mustMatch` was requiring the answer to name the WRONG variable, so the regression suite was holding a false fact in place — corrected and re-certified RED under drill
- 2026-09-11 · review@a4de0c0bc949 · `.claude/rules/ci.md` · **real** — the rule injected on every `scripts/**` edit still said `trust-certified`'s scope is parsed out of `trust-fast.yml`, the file the same lineage deleted. The rule that teaches at the moment of editing is the worst place for a dead address
- 2026-09-11 · review@a4de0c0bc949 · `scripts/check-trust-certified.mjs` · **real** — graded real though the reviewer filed it a nit, because of what it repeats: one of three `workflow` references was replaced and two left standing, INSIDE the edit meant to close the identical partial-fix finding logged an hour earlier for this same file. Twice in one session is not an oversight, it is a habit of not re-reading what the edit left
- 2026-09-11 · review@160d66d465ec · `.claude/rules/ci.md` · **real** — the SAME wrong claim ("Without `EVERDICT_TRUST_DATABASE_URL` these files skip") survived a few dozen lines below the edit that fixed it. Third partial fix in one session, and the one that made me stop fixing occurrences and `git grep` the claim across `CLAUDE.md`, `.claude/**`, `docs/**` and `evals/**` instead — which found exactly one remaining wrong statement and confirmed every recipe was already right, because `pnpm trust-fast` sets the flag itself
- 2026-09-11 · review@369892e97a52 · `docs/architecture/harness-drill-certificates.md` · **real** — and PREDICTIVE: it said the new certificate entry would carry every `neutralize` needle of the case it documents, so the exclusivity check would refuse to load the suite and block the push this diff was part of. It did, exactly. A page ABOUT a drill quoting that drill's needles is the leak the check exists for, and the entry now names the third needle without reproducing it
- 2026-09-11 · review@0a47017e23f2 · `findings/DISPOSITIONS.md` · **real** — a verdict written `**real**, and PREDICTIVE` put an adverb after the closing asterisks, so the ledger's regex missed it and `.filter(Boolean)` dropped the line. It graded a finding for a reader and for nothing else: `pnpm findings` reported 88 graded over 89 entry-shaped lines and said nothing about the difference. The reviewer found it by reading the REGEX, which is the only way it was findable. Repaired both halves — the line, and the reader, which now REFUSES an entry-shaped line that fails the full form
- 2026-09-11 · review@5b04a799c63b · `scripts/findings.mjs` · **real** — the repair for the silently-dropped entry GUESSED a shape (`- <date> · `) and so only covered the half of the space it had just removed: a line broken inside that prefix still vanished, reproducing the defect class inside the diff that closed it. Skill `code-review`'s residue rule, in the one file whose job is to measure that reviewer. Replaced with a total rule — the file declares where entries begin and every non-empty line after that marker must parse — so no shape is guessed and there is no other half to miss
- 2026-09-11 · review@89c07b752850 · `scripts/scan/run.mjs` · **real** — the SIBLING ledger reader. `scans/DISMISSED.md` shares the marker convention and still guessed a shape and dropped silently, so a malformed dismissal un-dismissed its finding: it returns as NEW on the next scan, nobody is told, and the findings-per-scan trend moves for an invisible reason. Nothing asked how many readers answer this same question until the reviewer did (skill `code-review` pass 4 — count the siblings, not the callers). Same total rule as its sibling now
- 2026-09-11 · review@89c07b752850 · `scripts/findings.mjs` · **real** — the total rule was total only BELOW the marker, so a well-formed disposition pasted into the prose above it was invisible again: the same defect moved from a bad shape to a bad position. Second consecutive repair here that covered the half it had just looked at. A misplaced entry is now named and refused
- 2026-09-11 · review@84f3992ae923 · `scripts/scan/run.mjs` · **real** — and the fourth consecutive finding of one class. The sibling ledger got HALF the rule ported: the malformed-line check, not the misplaced-above-marker one, so a dismissal above the marker still vanished silently. Four passes, each closing the half the previous had been looking at, every one caught by `pnpm review` and none by me. Repaired by DELETING the second copy — `scripts/marked-ledger.mjs` is one predicate both readers call, total in both directions, and a caller cannot take half of it (rule `protocol` L3)
- 2026-09-11 · scan@domain · `packages/domain/src/run/session-run.ts` · **real**, and worse than the scan rated it — filed `other`/low confidence as a cosmetic drift between `ttlSec` and `expiresAt`, verified by reading as a DECISION defeated. `ttlSec` is what `CampaignService`'s delegation budget refuses on (its own message says "was granted ${n}s"), and `extendSessionTransition` took the caller's raw value even on the path that deliberately holds the older, larger deadline. `POST /sandboxes/:id/touch` reaches it on both transports with `ttlSec` OPTIONAL, so an ordinary keep-alive overwrote the grant with the deployment default and a session with ten minutes left passed a two-minute frozen budget. The field obeys the same never-pull-it-in rule now. ⚠️ The scan's own reader-conditional ("any reader that derives remaining time from ttlSec") is what made this checkable — there are two, and one of them decides
- 2026-09-11 · scan@application · `packages/application-control/src/issue/github-issue-sync.ts` · **real** — the re-sync paths `.catch(() => [])` a failed GitHub comments list, and the domain REPLACES the stored thread with what arrives, so a rate-limit blip during a pull of an issue whose title had also changed erased its comment history permanently (echo suppression only skips an issue GitHub has not touched). Verified by reading all three links: the swallow, the watermark short-circuit, and the full-replacement write. The IMPORT path keeps its swallow — a fresh import has nothing to lose, and its comment says so — which is the residue half I checked before changing anything
- 2026-09-11 · scan@application · `packages/application-control/src/environment-adoption/environment-adoption-service.ts` · **real** — a capability-store outage was indistinguishable from a deleted capability, so `adopt()` answered 404 "not available to adopt" about an environment that exists. `resolve` is three-valued now and an unreadable store refuses as retryable upstream. ⚠️ The display half is NOT fixed and says so in the code: `available: false` still renders `unknown`, which reads as a revocation nobody performed — filed as `intent/2026-09-11-an-inventory-cannot-say-it-does-not-know/` because the third state's NAME is a product decision and the wrong word is worse than the current lie
- 2026-09-11 · scan@adapters · `packages/registry/src/pg-versioned-store.ts` · **real** — `registerReturning` declared `opts?: { preserveEntityOwner?, authority?: { expectedOwnerTeamId?, initialTeamId? } }` and read NONE of it (`grep 'opts\.'` in the file returned nothing), while its only caller never passed a fifth argument. Vestigial from arch-review 119, which threaded that precondition through and then REMOVED it — the guard went and the shape of it stayed, so a maintainer reads an ownership check that does not exist and wiring a caller would silently no-op. Deleted rather than implemented
- 2026-09-11 · scan@adapters · `packages/db/src/results/run-store.ts` · **real** — `InMemoryRunStore.parentAdmitsWork` read only the parent's status, and `peek` returns one `undefined` for both an absent scorecard and an unset field, so a dispatch naming a parent the store does not have was ADMITTED while `PARENT_AUTHORIZES`'s `EXISTS` clause refuses it. The twin was the more permissive of the two on an authorization axis — the one place that is worst — and no unit test built on the pair could see it (rule `testing`). Existence is asked separately now, with a counterexample and a mutation rung
- 2026-09-11 · scan@execution · `packages/backends/src/orchestrators/k8s.ts` · **real**, and the strongest finding of the rotation — `reclaimByName`, the arch-review 63 P1 repair for a create whose HTTP response was lost, filtered on `everdict.dev/job=<name>`. `grep` found exactly ONE occurrence of that key in the repository: this reader. No manifest wrote it, and `jobsByLabel` is a server-side `kubectl get jobs -A -l`, so the query matched nothing every time and the function answered `absent` for an object that existed — telling the operator nothing needed cleanup while an inert Job and its owned pull Secret stayed forever (`ttlSecondsAfterFinished` never fires on a suspended Job). ⚠️ The scan also found WHY nobody noticed: the counterexample written for this exact scenario had a `jobsByLabel` double taking NO argument and echoing back everything created, so the file's own correct assertions could never fire. Label written, double made to filter, and with the label removed those pre-existing assertions now go red
